@model IEnumerable<FirstApp.Models.Products>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.proName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.proCategory)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.proDescription)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.proMade)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.proPrice)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.proName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.proCategory)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.proDescription)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.proMade)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.proPrice)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
